For this smoke test, use the configured `supabase-mcp` server to inspect the
project with its database tools before making the requested change. Do not
replace the MCP call with a shell `curl` request or by editing the preserved
remote fixture; the purpose of this run is to verify the MCP transport.
