# build-cli-003-pg-cron-queue-workflow

- Source mode: `local-stack`
- Migration status: `runnable-pilot`

Detected scorer dependencies:

- database
- localCommands

The original source files remain under `evals/build-cli-003-pg-cron-queue-workflow/`. Generated task files
should be changed through `harbor/scripts/generate.mjs` so regeneration stays
deterministic.
