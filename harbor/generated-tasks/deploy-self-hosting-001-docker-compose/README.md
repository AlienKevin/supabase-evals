# deploy-self-hosting-001-docker-compose

- Source mode: `local-stack`
- Migration status: `runnable-pilot`

Detected scorer dependencies:

- localCommands
- localFiles

The original source files remain under `evals/deploy-self-hosting-001-docker-compose/`. Generated task files
should be changed through `harbor/scripts/generate.mjs` so regeneration stays
deterministic.
