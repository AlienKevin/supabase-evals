## Harbor parity submission contract

Complete the requested Supabase task using the supplied local and remote fixtures
and the available platform sidecar. Work on the task itself; do not invent a
successful result without checking the available state.

In addition to your normal final response, write a concise report of what you
found or changed to `/app/answer.md`. When the task asks you to create or edit
files, create them under `/app` at the paths implied by the task and verify the
result before finishing. The hidden Harbor verifier is an adapter around the
upstream Supabase scorer; it is not a replacement rubric. Preserve the source
task's observable behavior, files, SQL state, tool history, transcript, and
judge requirements. Do not optimize for a particular answer string or write
to any grading artifact.
